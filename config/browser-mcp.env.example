# Browser MCP connection details (from the extension UI)
BROWSER_MCP_HOST=127.0.0.1
BROWSER_MCP_PORT=
BROWSER_MCP_TOKEN=

# Optional: dedicated Chrome profile directory
BROWSER_MCP_PROFILE_DIR=$HOME/.mcp-browser-control-profile
